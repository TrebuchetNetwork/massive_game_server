[advisories]
# Current upstream crates are flagged as unmaintained with no drop-in replacements
# available in our dependency tree yet. Track and remove each ignore as dependencies
# are upgraded.
ignore = [
    "RUSTSEC-2025-0134", # rustls-pemfile (direct; unmaintained warning, no drop-in replacement yet)
    "RUSTSEC-2025-0141", # bincode (transitive via webrtc)
]
