# Template only — do not commit real values here. Fill these in via your
# secret-management flow (kubectl create secret, sealed-secrets, sops, vault,
# etc.) at apply time, never by editing this file with real credentials.
# Empty is the safe default: the app fails closed (admin/arena routes stay
# disabled) rather than accepting a known, guessable placeholder as if it
# were a real secret.
apiVersion: v1
kind: Secret
metadata:
  name: massive-game-server-secrets
  labels:
    app: massive-game-server
type: Opaque
stringData:
  MGS_ADMIN_BEARER_TOKEN: ""
  MGS_LIVE_REPLAY_DISPUTE_SIGNING_KEY: ""
  MGS_REDIS_URL: ""
