global:
  resolve_timeout: 1m

route:
  receiver: webhook-receiver
  group_by: ['alertname']
  group_wait: 0s
  group_interval: 5s
  repeat_interval: 1h

receivers:
  - name: webhook-receiver
    webhook_configs:
      - url: http://alert-webhook:18081/alerts
        send_resolved: true
